class cartModel {
 constructor(quantity, productPrice, productTitle, sum, cartIndex) {
  this.quantity = quantity;
  this.productPrice = productPrice;
  this.productTitle = productTitle;
  this.sum = sum;
  this.cartIndex = cartIndex
  }
}

export default cartModel;